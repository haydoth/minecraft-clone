#pragma once
class block_manager
{
	// important!! block indexing needs to extremely ergonomic, so do
	// we abstract away the concept of chunks completely or only partly?

	// pseudocode api
	
	// block at(glm::ivec3);
};

